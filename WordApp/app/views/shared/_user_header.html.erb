<div class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">WordApp</a>
    </div>
    <ul class="nav navbar-nav">
      <li class="active"><a href="/">Home</a></li>
    </ul>

    <% if user_signed_in? %>
    <p class="navbar-text pull-right"><%= current_user.email %>| <a href=""><%= link_to "Sign Out",destroy_user_session_path, :method => :delete %></a></p>
  	<% else %>
  		<p class="navbar-text pull-right">  <%= link_to "Sign Up", new_user_registration_path %></p>
  		<p class="navbar-text pull-right"> or </p>
  		<p class="navbar-text pull-right"><%= link_to "Sign in", new_user_session_path %> </p>
  	<% end %>
  </div>
</div>


